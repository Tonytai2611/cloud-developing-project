import React from 'react';
import { MessageCircle, Send } from 'lucide-react';

export default function ChatThread({ chat }) {
  if (!chat.selectedUser) return <section className="grid min-h-[520px] place-items-center bg-slate-50/50 p-8 text-center"><div><span className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-teal-50 text-teal-600"><MessageCircle className="h-9 w-9" /></span><h2 className="mt-5 font-serif text-2xl font-bold text-slate-900">Select a conversation</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-slate-500">Choose a customer from the conversation list to view history and send a message.</p></div></section>;
  return <section className="flex min-h-[600px] min-w-0 flex-col bg-white"><header className="flex items-center gap-3 border-b border-slate-200 px-5 py-4"><span className="relative grid h-11 w-11 place-items-center rounded-full bg-teal-100 font-bold text-teal-800">{chat.selectedUser.avatar}<span className={`absolute bottom-0 right-0 h-3 w-3 rounded-full border-2 border-white ${chat.selectedUser.status === 'Online' ? 'bg-emerald-500' : 'bg-slate-400'}`} /></span><div className="min-w-0"><h2 className="truncate font-bold text-slate-900">{chat.selectedUser.name || chat.selectedUser.email}</h2><p className="truncate text-xs text-slate-500">{chat.selectedUser.email} · {chat.selectedUser.status}</p></div></header><div ref={chat.messagesContainerRef} onScroll={chat.handleScroll} className="flex-1 space-y-5 overflow-y-auto bg-slate-50/60 p-4 sm:p-6">{chat.messages.length ? chat.messages.map((message, index) => <div key={message.id || index} className={`flex items-end gap-2 ${message.isAdmin ? 'justify-end' : 'justify-start'}`}>{!message.isAdmin && <span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-slate-200 text-xs font-bold text-slate-700">{message.avatar}</span>}<div className={`max-w-[82%] sm:max-w-[70%] ${message.isAdmin ? 'text-right' : 'text-left'}`}><p className="mb-1 px-1 text-[11px] text-slate-400">{message.senderName} · {chat.formatTimestamp(message.timestamp)}</p><div className={`rounded-2xl px-4 py-3 text-left text-sm leading-6 shadow-sm ${message.isAdmin ? 'rounded-br-sm bg-teal-700 text-white' : 'rounded-bl-sm border border-slate-200 bg-white text-slate-700'}`}>{message.text}</div></div></div>) : <div className="grid h-full place-items-center text-center text-sm text-slate-400">No messages yet. Start the conversation.</div>}<div ref={chat.messagesEndRef} /></div><form onSubmit={(event) => { event.preventDefault(); chat.sendMessage(); }} className="flex items-center gap-2 border-t border-slate-200 p-4"><input value={chat.newMessage} onChange={(event) => chat.setNewMessage(event.target.value)} placeholder="Type a message…" className="h-12 min-w-0 flex-1 rounded-xl border border-slate-200 bg-slate-50 px-4 text-sm outline-none focus:border-teal-600 focus:ring-2 focus:ring-teal-100" /><button type="submit" disabled={!chat.newMessage.trim() || !chat.isConnected} aria-label="Send message" className="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-teal-700 text-white hover:bg-teal-800 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-teal-600 focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:bg-slate-300"><Send className="h-5 w-5" /></button></form></section>;
}

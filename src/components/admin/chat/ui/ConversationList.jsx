import React from 'react';
import { MessageCircle, Search } from 'lucide-react';

export default function ConversationList({ chat }) {
  return <aside className="flex min-h-0 flex-col border-b border-slate-200 bg-white xl:border-b-0 xl:border-r"><div className="border-b border-slate-200 p-4"><label className="relative block"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={chat.searchTerm} onChange={(event) => chat.setSearchTerm(event.target.value)} placeholder="Search users or messages…" className="h-11 w-full rounded-xl border border-slate-200 bg-slate-50 pl-10 pr-3 text-sm outline-none focus:border-teal-600 focus:ring-2 focus:ring-teal-100" /></label><div className="mt-3 flex items-center justify-between"><p className="font-bold text-slate-900">Conversations</p><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${chat.isConnected ? 'bg-emerald-50 text-emerald-700' : 'bg-red-50 text-red-700'}`}>{chat.isConnected ? 'Connected' : 'Offline'}</span></div></div><div className="max-h-72 flex-1 overflow-y-auto xl:max-h-none">{chat.loading ? <p className="p-6 text-center text-sm text-slate-500">Loading conversations…</p> : chat.filteredUsers.length ? chat.filteredUsers.map((user) => { const active = chat.selectedUser?.email === user.email; return <button key={user.id || user.email} type="button" onClick={() => chat.selectUser(user)} className={`flex w-full items-start gap-3 border-b border-slate-100 px-4 py-4 text-left ${active ? 'bg-teal-50' : 'hover:bg-slate-50'}`}><span className={`relative grid h-11 w-11 shrink-0 place-items-center rounded-full font-bold ${active ? 'bg-teal-700 text-white' : 'bg-slate-100 text-slate-700'}`}>{user.avatar}<span className={`absolute bottom-0 right-0 h-3 w-3 rounded-full border-2 border-white ${user.status === 'Online' ? 'bg-emerald-500' : 'bg-slate-400'}`} /></span><span className="min-w-0 flex-1"><span className="flex items-center justify-between gap-2"><span className="truncate font-bold text-slate-900">{user.name || user.email}</span>{user.unread > 0 && <span className="grid h-6 min-w-6 place-items-center rounded-full bg-red-500 px-1 text-xs font-bold text-white">{user.unread}</span>}</span><span className="mt-1 block truncate text-xs text-slate-500">{user.lastMessage || user.email}</span></span></button>; }) : <div className="p-8 text-center"><MessageCircle className="mx-auto h-8 w-8 text-slate-300" /><p className="mt-3 text-sm text-slate-500">No conversations found.</p></div>}</div></aside>;
}
